{
  "name": "recipe-recommendation-app",
  "version": "1.0.0",
  "description": "Recipe Recommendation System",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"python backend/main.py\"",
    "build": "npm run build --prefix frontend",
    "start": "python backend/main.py"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}